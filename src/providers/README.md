# providers/

React context sağlayıcıları için rezerve edilmiştir (örn. `TooltipProvider`
sarmalayıcı, ileride tema/i18n provider'ı). Şu an `TooltipProvider`
doğrudan `src/app/dashboard/layout.tsx` içinde kullanılıyor; ayrı bir
sarmalayıcıya çıkarılması ayrı bir refactor adımıdır, henüz yapılmadı.
