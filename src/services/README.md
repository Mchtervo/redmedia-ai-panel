# services/

Birden fazla feature'ı kesen orkestrasyon mantığı (örn. `AttributionService`
— leads + ads + sales verisini birleştirir). Tek bir feature'a özgü servis
mantığı burada değil, ilgili `features/<isim>/services/` içinde kalır.

Henüz kod eklenmedi.
