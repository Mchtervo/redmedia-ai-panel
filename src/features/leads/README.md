# features/leads

**Menü:** Leads (`/dashboard/leads`)

**Sahip olduğu tablo(lar):** `lead_profiles`, `lead_events`

**Bağımlılık:** `features/contacts` (contact_id), `features/ads` (source_campaign_id, source_ad_id)

Henüz kod eklenmedi. Standart iç yapı için `src/features/README.md`'ye bakın.
