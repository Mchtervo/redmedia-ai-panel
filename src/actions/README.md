# actions/

Yalnızca global/cross-feature Server Action'lar (örn. `signOutAction`).
Feature'a özel mutasyonlar (örn. `createLeadAction`) burada değil, ilgili
`features/<isim>/actions/` içinde kalır.

Henüz kod eklenmedi.
