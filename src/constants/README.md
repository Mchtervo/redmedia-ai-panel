# constants/

Uygulama geneli sabitler (rota yolları, sabit enum metinleri). Mevcut
`src/lib/navigation.ts` (sidebar menü tanımı) feature-agnostic bir UI
yardımcısı olduğu için şimdilik `lib/` içinde kalır; ileride rota sabitleri
büyürse buraya taşınabilir.

Henüz kod eklenmedi.
